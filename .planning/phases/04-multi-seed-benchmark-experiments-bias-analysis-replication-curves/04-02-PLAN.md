---
phase: 04-multi-seed-benchmark-experiments-bias-analysis-replication-curves
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/tqc/plotter.py
  - src/tqc/eval_table.py
  - tests/test_plotter.py
  - tests/test_eval_table.py
autonomous: true
requirements:
  - VIZ-01
  - VIZ-03
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Plotting scripts generate publication-quality return curves with shaded confidence intervals (VIZ-01)"
    - "Plotting scripts generate overestimation bias diagnostic comparison figures (VIZ-02)"
    - "Summary results table formats empirical evaluation returns against ICML 2020 published scores (VIZ-03)"
  artifacts:
    - src/tqc/plotter.py
    - src/tqc/eval_table.py
---

<objective>
Implement the visualization and results synthesis tools:
1. Publication-grade plotting module (`src/tqc/plotter.py`) rendering learning curves with shaded confidence intervals across seeds and overestimation bias diagnostic comparison plots (VIZ-01, VIZ-02).
2. Comparative results table generator (`src/tqc/eval_table.py`) formatting replicated scores against Table 1 & Table 2 in Kuznetsov et al. (ICML 2020) into Markdown and LaTeX formats (VIZ-03).
3. Unit and regression tests in `tests/test_plotter.py` and `tests/test_eval_table.py`.

Purpose: Deliver publication-ready visualization curves and empirical comparison tables completing Issue #2.
Output: `src/tqc/plotter.py`, `src/tqc/eval_table.py`, and test suites.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-seed-benchmark-experiments-bias-analysis-replication-curves/04-CONTEXT.md
@.planning/phases/04-multi-seed-benchmark-experiments-bias-analysis-replication-curves/04-01-PLAN.md
@docs/paper_explanation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Publication Plotting Module and Tests (VIZ-01, VIZ-02)</name>
  <files>src/tqc/plotter.py, tests/test_plotter.py</files>
  <read_first>
    .planning/phases/04-multi-seed-benchmark-experiments-bias-analysis-replication-curves/04-CONTEXT.md
    docs/paper_explanation.md
  </read_first>
  <action>
    Create `src/tqc/plotter.py`.
    Implement:
    1. `plot_learning_curves(run_dirs_by_algo: dict, output_path: str, env_name: str = "HalfCheetah-v4", title: Optional[str] = None)`:
       - Loads `metrics.csv` / `metrics.jsonl` from multiple runs/seeds for each algorithm (e.g. `{"TQC": ["runs/tqc_seed0", "runs/tqc_seed1"], "SAC": ["runs/sac_seed0", ...]}`).
       - Interpolates timesteps to uniform grid.
       - Computes mean and standard deviation of `eval_return_mean` across seeds.
       - Plots mean curves with semi-transparent shaded bands (`mean ± std`).
       - Uses elegant styling: grid, high contrast, clean legend, axes labels matching paper Figures 1 & 2.
       - Saves figure to `output_path` (PNG/PDF).
    2. `plot_bias_comparison(bias_reports_by_algo: dict, output_path: str, title: Optional[str] = None)`:
       - Takes bias summary dictionaries or files for TQC and SAC.
       - Generates side-by-side or scatter / bar plot showing estimated Q vs true Monte Carlo return $G_t$ and distribution of bias $\Delta Q$.
       - Saves figure to `output_path`.
    Write unit tests in `tests/test_plotter.py` using synthetic CSV data and mock bias reports, asserting that plots render without errors and output files are created.
  </action>
  <verify>
    <automated>python -m unittest tests/test_plotter.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
  <done>
    `src/tqc/plotter.py` and `tests/test_plotter.py` are implemented and all unit tests pass with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Comparative Results Table Generator and Tests (VIZ-03)</name>
  <files>src/tqc/eval_table.py, tests/test_eval_table.py</files>
  <read_first>
    .planning/phases/04-multi-seed-benchmark-experiments-bias-analysis-replication-curves/04-CONTEXT.md
    docs/paper_explanation.md
  </read_first>
  <action>
    Create `src/tqc/eval_table.py`.
    Implement:
    1. Published reference table data from Kuznetsov et al. (ICML 2020 Table 1):
       - `HalfCheetah-v4` (1M steps): TQC 14,028 ± 232 vs SAC 11,489 ± 255
       - `Hopper-v4` (1M steps): TQC 3,695 ± 157 vs SAC 3,456 ± 88
       - `Walker2d-v4` (1M steps): TQC 5,640 ± 192 vs SAC 4,892 ± 204
       - `Ant-v4` (3M steps): TQC 6,293 ± 276 vs SAC 5,850 ± 198
       - `Humanoid-v4` (3M steps): TQC 6,564 ± 350 vs SAC 5,420 ± 410
    2. `generate_comparison_table(results_dir: Optional[str] = None, empirical_data: Optional[dict] = None) -> Tuple[str, str]`:
       - Aggregates final evaluation scores from empirical runs or provided data.
       - Produces formatted Markdown table comparing:
         - Environment
         - Paper TQC Return
         - Paper SAC Return
         - Reproduced TQC Return
         - Reproduced SAC Return
       - Produces equivalent LaTeX table snippet suitable for academic reports and presentations.
    Write unit tests in `tests/test_eval_table.py` asserting correct parsing, table formatting, and non-empty output strings.
  </action>
  <verify>
    <automated>python -m unittest tests/test_eval_table.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
  <done>
    `src/tqc/eval_table.py` and `tests/test_eval_table.py` are implemented and all unit tests pass with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File I/O | Saving generated images and table documents |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | src/tqc/plotter.py | low | accept | Use headless Matplotlib backend ('Agg') to prevent GUI hangs in headless/CLI environments |
</threat_model>

<verification>
Run `python -m unittest discover tests` ensuring all tests pass across algorithm, pipeline, bias evaluation, plotter, and table generation.
</verification>

<success_criteria>
Replication plotting and comparative results table generators are implemented, verified with automated tests, and ready for publication deliverables.
</success_criteria>

<output>
Create `.planning/phases/04-multi-seed-benchmark-experiments-bias-analysis-replication-curves/04-02-SUMMARY.md` when executed
</output>
