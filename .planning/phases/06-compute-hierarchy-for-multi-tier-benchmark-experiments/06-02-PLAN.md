---
phase: 06-compute-hierarchy-for-multi-tier-benchmark-experiments
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - configs/benchmark_matrix.yaml
  - src/tqc/remote/queue_manager.py
  - scripts/sync_remote_artifacts.py
  - scripts/run_remote_experiment.py
  - docs/remote_execution.md
  - tests/test_remote_queue.py
  - tests/test_remote_sync.py
autonomous: true
requirements:
  - Issue #4
  - Issue #7
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-04: Asynchronous polling via Kaggle CLI with progress log streaming and detached background execution support"
    - "D-05: Automated CLI sync script (scripts/sync_remote_artifacts.py) utilizing Kaggle CLI kaggle kernels output"
    - "D-06: Tiered retention policy bundles periodic checkpoints and final models into compressed .tar.gz archive"
    - "D-07: Timestamped run directories with platform prefixes and SHA256 integrity validation"
    - "D-08: Automatic post-sync verification of metrics.csv and immediate CLI triggers for plotting and progression videos"
    - "D-09: Dual configuration support for declarative YAML (configs/benchmark_matrix.yaml) and CLI flags"
    - "D-10: Scope focus pilots Kaggle execution on HalfCheetah-v4 (Issue #7) before expanding to full suite (Issue #4)"
    - "D-11: Dual-slot queue manager tracks active jobs, polls Kaggle status, and enforces max 2 concurrent GPU kernels"
    - "D-12: TQC-only experimental matrix evaluates empirical reproduction curves against published ICML 2020 numbers"
    - "D-17: HalfCheetah pilot executes 1M steps with 50k checkpoint frequency and 5k eval frequency preceded by 100-step dry run"
    - "Unified remote experiment runner orchestrates pre-flight smoke test, kernel push, background monitor, and sync"
    - "All remote queue and sync unit tests pass deterministically"
    - "Documentation comprehensively details remote compute setup, queue management, and HalfCheetah pilot workflow"
  artifacts:
    - configs/benchmark_matrix.yaml
    - src/tqc/remote/queue_manager.py
    - scripts/sync_remote_artifacts.py
    - scripts/run_remote_experiment.py
    - docs/remote_execution.md
    - tests/test_remote_queue.py
    - tests/test_remote_sync.py
---

<objective>
Implement the dual-slot queue manager, artifact sync & integrity verification protocol, unified experiment runner, declarative configuration, and documentation:
1. D-09, D-10, D-12, D-17: Author `configs/benchmark_matrix.yaml` supporting both named presets (`pilot` on HalfCheetah-v4 1M steps, `smoke`, `benchmark_suite`) and CLI parameterized flag overrides for TQC-only benchmark reproduction.
2. D-04 & D-11: Implement `src/tqc/remote/queue_manager.py` enforcing Kaggle's dual-slot GPU constraint (max 2 concurrent kernels), polling kernel statuses (`queued`, `running`, `complete`, `error`), and dynamically dispatching queued jobs.
3. D-05, D-06, D-07, D-08: Implement `scripts/sync_remote_artifacts.py` pulling remote outputs via Kaggle CLI, unpacking `artifacts.tar.gz` into timestamped platform run dirs `runs/<platform>_<env>_<seed>_<timestamp>/`, validating SHA256 integrity against `checksums.sha256`, verifying `metrics.csv` schema, and providing immediate commands for plotting curves and rendering progression videos.
4. D-04 & D-17: Implement `scripts/run_remote_experiment.py` orchestrating the complete lifecycle: pre-flight smoke test -> kernel packaging & push -> queue dispatch -> detached monitoring -> artifact synchronization.
5. Author `docs/remote_execution.md` providing end-to-end user guidance for remote execution, credentials setup, queue management, and HalfCheetah pilot reproduction.
6. Author unit tests in `tests/test_remote_queue.py` and `tests/test_remote_sync.py`.

Purpose: Provide a production-ready remote orchestration, queuing, and artifact collection system for continuous control benchmark experiments.
Output: Queue manager, sync script, unified runner, config preset, test suites, and remote execution documentation.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
@.planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
@.planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dual-Slot Queue Manager, Remote Artifact Sync Script & Test Suites</name>
  <files>src/tqc/remote/queue_manager.py, scripts/sync_remote_artifacts.py, tests/test_remote_queue.py, tests/test_remote_sync.py</files>
  <read_first>
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
    src/tqc/remote/kaggle_auth.py
    src/tqc/remote/kaggle_packager.py
  </read_first>
  <acceptance_criteria>
    - D-11: `src/tqc/remote/queue_manager.py` defines `DualSlotQueueManager` enforcing max concurrent slot limit (default 2)
    - D-04: Asynchronous status polling detects `queued`, `running`, `complete`, and `error` states
    - D-05 & D-06: `scripts/sync_remote_artifacts.py` defines `download_kernel_output` and `verify_and_extract_artifacts`
    - D-07: SHA256 integrity validation checks every extracted file against `checksums.sha256`
    - D-08: `metrics.csv` schema validation verifies headers and triggers plotting/progression commands
    - `python -m unittest tests/test_remote_queue.py tests/test_remote_sync.py` exits with 0 and zero failed tests
  </acceptance_criteria>
  <action>
    1. Implement `src/tqc/remote/queue_manager.py`:
       - Class `DualSlotQueueManager`:
         - Capacity: default 2 concurrent active GPU jobs (configurable).
         - Methods:
           - D-11: `add_job(job_id: str, kernel_dir: str, env_id: str, seed: int) -> None`: Enqueues an experiment.
           - `get_active_jobs() -> list[dict]`: Returns running / queued jobs.
           - D-04: `poll_job_status(kernel_id: str) -> str`: Checks status via Kaggle CLI / API (`queued`, `running`, `complete`, `error`).
           - D-11: `step_queue() -> dict`: Checks running jobs, cleans up completed/failed jobs, and dispatches pending jobs up to the max concurrent slot limit.
           - D-04: `monitor_until_completion(poll_interval: int = 30, on_complete_callback = None)`: Loops until all queued and active jobs have finished, logging live status updates.
    2. Implement `scripts/sync_remote_artifacts.py`:
       - D-05: `download_kernel_output(kernel_id: str, download_dir: str) -> bool`: Calls `kaggle kernels output <kernel_id> -p <download_dir>`.
       - D-06, D-07, D-08: `verify_and_extract_artifacts(download_dir: str, target_run_dir: str) -> dict`:
         - Finds `artifacts.tar.gz` and unpacks it.
         - Reads `checksums.sha256` and computes SHA256 hashes of extracted files to guarantee file integrity (D-07).
         - Verifies `metrics.csv` contains standard columns (`step`, `eval_return_mean`, etc.) (D-08).
       - Generates helpful downstream terminal commands (D-08):
         - Return curve plotting: `python -m src.tqc.plotter --log-dirs <target_run_dir>`
         - Progression video generation: `python scripts/run_halfcheetah_progression.py --skip-train --checkpoint-dir <target_run_dir>/checkpoints`
    3. Author unit tests:
       - `tests/test_remote_queue.py`:
         - Test slot limits (never exceeds 2 concurrent running jobs).
         - Test job state transitions and dequeuing as jobs complete.
       - `tests/test_remote_sync.py`:
         - Test archive extraction and SHA256 integrity validation.
         - Test error handling on corrupted archives or mismatched checksums.
         - Test `metrics.csv` schema validation.
  </action>
  <verify>
    <automated>python -m unittest tests/test_remote_queue.py tests/test_remote_sync.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
  <done>
    DualSlotQueueManager enforces concurrency bounds, artifact sync verifies SHA256 hashes and metrics schema, and all unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Declarative Matrix Config, Unified Experiment Runner & Remote Documentation</name>
  <files>configs/benchmark_matrix.yaml, scripts/run_remote_experiment.py, docs/remote_execution.md</files>
  <read_first>
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
    src/tqc/remote/queue_manager.py
    scripts/sync_remote_artifacts.py
  </read_first>
  <acceptance_criteria>
    - D-09, D-10, D-12: `configs/benchmark_matrix.yaml` contains `pilot`, `smoke`, and `benchmark_suite` sections with correct envs, seeds, and steps
    - D-17: Pilot configuration specifies HalfCheetah-v4, seed 42, 1M steps, 50k checkpoint frequency, 5k eval frequency
    - D-04: `scripts/run_remote_experiment.py --help` outputs runnable CLI documentation with all flags including `--detach`
    - `docs/remote_execution.md` contains comprehensive setup instructions (>1000 characters)
    - All validation commands execute with return code 0
  </acceptance_criteria>
  <action>
    1. D-09, D-10, D-12, D-17: Create `configs/benchmark_matrix.yaml`:
       - Define preset `pilot` (D-10, D-17):
         - Environment: `HalfCheetah-v4`
         - Seed: `42`
         - Total steps: `1000000` (1M steps, paper spec)
         - Checkpoint frequency: `50000`
         - Evaluation frequency: `5000`
         - Batch size: `256`
       - Define preset `smoke`:
         - Environment: `HalfCheetah-v4`
         - Seed: `42`
         - Total steps: `100`
         - Checkpoint frequency: `50`
       - Define preset `benchmark_suite` (D-10, D-12):
         - Environments: `[HalfCheetah-v4, Hopper-v4, Walker2d-v4, Ant-v4, Humanoid-v4]`
         - Seeds: `[42, 43, 44, 45, 46]`
         - Total steps: `1000000` (Humanoid: `3000000`)
    2. Implement `scripts/run_remote_experiment.py`:
       - CLI arguments: `--preset <name>`, `--env <env_id>`, `--seed <int>`, `--steps <int>`, `--dry-run`, `--skip-smoke`, `--detach`, `--sync-only <kernel_id>`.
       - Workflow:
         - Resolve Kaggle credentials (with prompt if missing).
         - Unless `--skip-smoke`, run 100-step local pre-flight smoke test (D-17).
         - Package kernel code and metadata into temporary or persistent build directory.
         - Push kernel to Kaggle via Kaggle CLI.
         - If `--detach`, log kernel ID and exit with monitoring instructions (D-04).
         - Otherwise, monitor via `DualSlotQueueManager`, and on completion automatically trigger `sync_remote_artifacts.py`.
    3. Author `docs/remote_execution.md`:
       - Architecture overview of the compute hierarchy (Tier-1 Kaggle GPU > Tier-2 Colab > Tier-3 Local GPU > Tier-4 Local CPU).
       - Kaggle API token setup instructions (`~/.kaggle/kaggle.json` & `.env`).
       - Step-by-step instructions for running the HalfCheetah-v4 pilot run (GitHub Issue #7).
       - Dual-slot queue management and status monitoring commands.
       - Artifact synchronization, checksum verification, and progression visualization workflow.
  </action>
  <verify>
    <automated>python scripts/run_remote_experiment.py --help && python -c "import yaml; cfg = yaml.safe_load(open('configs/benchmark_matrix.yaml')); assert 'pilot' in cfg and 'benchmark_suite' in cfg; print('Config verified!')" && python -c "assert open('docs/remote_execution.md', encoding='utf-8').read().startswith('# Remote Compute Execution') and len(open('docs/remote_execution.md', encoding='utf-8').read()) > 1000; print('Docs verified!')"</automated>
    <fails_when>non-zero exit code or failed validation</fails_when>
  </verify>
  <done>
    Benchmark matrix YAML is authored, run_remote_experiment CLI is operational, and remote execution documentation is complete.
  </done>
</task>

</tasks>

<artifacts_produced>
### Artifacts this phase produces
- Scripts & Runners: `scripts/run_remote_experiment.py`, `scripts/sync_remote_artifacts.py`
- Modules & Classes: `src/tqc/remote/queue_manager.py` (`DualSlotQueueManager`)
- Configurations: `configs/benchmark_matrix.yaml`
- Documentation: `docs/remote_execution.md`
- Test Suites: `tests/test_remote_queue.py`, `tests/test_remote_sync.py`
</artifacts_produced>

<verification>
Automated test gates:
- `python -m unittest tests/test_remote_queue.py tests/test_remote_sync.py`
- `python scripts/run_remote_experiment.py --help`
- YAML config integrity check and markdown documentation verification.
</verification>
