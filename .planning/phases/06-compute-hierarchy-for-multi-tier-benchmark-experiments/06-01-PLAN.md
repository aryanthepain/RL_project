---
phase: 06-compute-hierarchy-for-multi-tier-benchmark-experiments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tqc/remote/__init__.py
  - src/tqc/remote/kaggle_auth.py
  - src/tqc/remote/kaggle_packager.py
  - tests/test_remote_auth.py
  - tests/test_remote_packager.py
autonomous: true
requirements:
  - Issue #4
  - Issue #7
estimate:
  tokens: 35000
  raw_tokens: 18000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01: Compute Tier Prioritization establishes Tier-1 Kaggle Headless Kernels > Tier-2 Colab > Tier-3 Local CUDA > Tier-4 Local CPU fallback"
    - "D-02: Project code deployed to Kaggle via packaged code archive and kernel metadata JSON pushed via Kaggle CLI"
    - "D-03: Automatic CUDA auto-detection across all modules with explicit --device override and CPU fallback"
    - "D-13: Automated pre-flight validation runs a 100-step local smoke test verifying environment creation, tensors, and checkpointing"
    - "D-14: Adaptive replay buffer capacity supports 1M transitions on remote tier and 100k-250k on local smoke runs"
    - "D-15: Exact paper batch size 256 with PyTorch CUDA optimizations enabled (torch.backends.cudnn.benchmark = True)"
    - "D-16: Dual credential resolver detects ~/.kaggle/kaggle.json and .env fallback with automated validation and interactive prompt support"
    - "Automated unit tests for auth and packager pass deterministically"
  artifacts:
    - src/tqc/remote/__init__.py
    - src/tqc/remote/kaggle_auth.py
    - src/tqc/remote/kaggle_packager.py
    - tests/test_remote_auth.py
    - tests/test_remote_packager.py
---

<objective>
Implement the remote compute foundation, Kaggle authentication resolver, kernel packager, and local pre-flight smoke validator:
1. D-01 & D-16: Create `src/tqc/remote/__init__.py` and `src/tqc/remote/kaggle_auth.py` supporting dual-credential resolution (`~/.kaggle/kaggle.json` and `.env`), programmatic validation against Kaggle API, and interactive prompt fallback if credentials are unset.
2. D-02, D-03, D-14, D-15: Create `src/tqc/remote/kaggle_packager.py` to generate `kernel-metadata.json`, bundle project source code into remote payload archives, construct headless remote execution entrypoints (`remote_entrypoint.py`) with automatic dependency installation (`gymnasium[mujoco]`, `mujoco`), execution of `train_tqc`, CUDA device detection, adaptive replay buffer, batch size 256, and creation of compressed `artifacts.tar.gz` upon completion.
3. D-13: Implement pre-flight local smoke test capability (100-step dry run) ensuring environment instantiation, CUDA/CPU tensor flow, and checkpoint saving succeed before cloud dispatch.
4. Author comprehensive unit tests in `tests/test_remote_auth.py` and `tests/test_remote_packager.py`.

Purpose: Establish robust, reproducible packaging and authentication foundation for offloading TQC training to remote Kaggle GPU instances.
Output: `src/tqc/remote/` package with auth, packager, and verified unit test suites.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
@.planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kaggle Credential Detection, Validation & Interactive Prompting</name>
  <files>src/tqc/remote/__init__.py, src/tqc/remote/kaggle_auth.py, tests/test_remote_auth.py</files>
  <read_first>
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - D-16: `src/tqc/remote/kaggle_auth.py` exports `resolve_kaggle_credentials`, `validate_kaggle_credentials`, and `ensure_kaggle_cli_installed`
    - D-16: Credential resolution reads `~/.kaggle/kaggle.json` first, falls back to `KAGGLE_USERNAME`/`KAGGLE_KEY` env vars
    - D-16: Missing credentials trigger prompt when `prompt_if_missing=True` or raise `KaggleAuthError`
    - `python -m unittest tests/test_remote_auth.py` exits with 0 and zero failed tests
  </acceptance_criteria>
  <action>
    1. Create `src/tqc/remote/__init__.py` exposing remote modules.
    2. Implement `src/tqc/remote/kaggle_auth.py`:
       - D-16: `resolve_kaggle_credentials(prompt_if_missing: bool = False) -> tuple[str, str]`:
         - Checks `~/.kaggle/kaggle.json` first (JSON containing `username` and `key`).
         - Checks environment variables `KAGGLE_USERNAME` and `KAGGLE_KEY` (and reads `.env` if present).
         - If both are missing and `prompt_if_missing` is True, interactively prompts the user for Kaggle Username and API Key, validates them, and writes `~/.kaggle/kaggle.json` (chmod 600 / secure).
         - If credentials cannot be resolved, raises informative `KaggleAuthError`.
       - D-16: `validate_kaggle_credentials(username: str, api_key: str) -> bool`:
         - Invokes Kaggle API client or CLI test (`kaggle datasets list` or API `authenticate()`) to verify credentials are active and functional.
       - D-16: `ensure_kaggle_cli_installed() -> bool`:
         - Verifies `kaggle` CLI binary is available on PATH or callable via Python.
    3. Author unit tests in `tests/test_remote_auth.py`:
       - Test credential loading from `kaggle.json`.
       - Test credential loading from environment variables.
       - Test error raising on missing credentials.
       - Test credential file writing and mock authentication verification.
  </action>
  <verify>
    <automated>python -m unittest tests/test_remote_auth.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
  <done>
    `kaggle_auth.py` accurately resolves credentials across JSON and environment sources with deterministic unit test coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Kaggle Kernel Packager, Remote Entrypoint Builder & Pre-Flight Smoke Validator</name>
  <files>src/tqc/remote/kaggle_packager.py, tests/test_remote_packager.py</files>
  <read_first>
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-CONTEXT.md
    .planning/phases/06-compute-hierarchy-for-multi-tier-benchmark-experiments/06-RESEARCH.md
    src/tqc/train.py
  </read_first>
  <acceptance_criteria>
    - D-01 & D-02: `src/tqc/remote/kaggle_packager.py` exports `build_kernel_metadata`, `create_remote_entrypoint`, `package_kernel_directory`, and `run_local_preflight_smoke`
    - D-02: `build_kernel_metadata` returns valid JSON dictionary with `enable_gpu=True` and `enable_internet=True`
    - D-03, D-14, D-15: `create_remote_entrypoint` generates python code string containing package installations, training loop call, and `artifacts.tar.gz` creation
    - D-13: `run_local_preflight_smoke` executes 100 steps in <10 seconds and verifies checkpoint creation
    - `python -m unittest tests/test_remote_packager.py` exits with 0 and zero failed tests
  </acceptance_criteria>
  <action>
    1. Implement `src/tqc/remote/kaggle_packager.py`:
       - D-02: `build_kernel_metadata(username: str, kernel_slug: str, title: str, code_file: str = "remote_entrypoint.py", enable_gpu: bool = True, is_private: bool = True) -> dict`:
         - Generates standard `kernel-metadata.json` dictionary adhering to Kaggle specifications (`enable_gpu=True`, `enable_internet=True`, `language=python`, `kernel_type=script`).
       - D-01, D-03, D-14, D-15: `create_remote_entrypoint(env_id: str, seed: int, total_timesteps: int, checkpoint_freq: int, eval_freq: int, exp_name: str, device: str = "cuda", buffer_size: int = 1000000, batch_size: int = 256) -> str`:
         - Generates a standalone Python script `remote_entrypoint.py` that:
           - Automatically installs necessary packages: `pip install gymnasium[mujoco] mujoco`.
           - Unpacks the bundled `tqc_source.tar.gz` or adds `src/` to `sys.path`.
           - Instantiates environment and verifies CUDA availability (D-03).
           - Configures buffer size 1M (D-14) and batch size 256 with PyTorch cudnn benchmark (D-15).
           - Invokes `train_tqc(env_id=..., seed=..., total_timesteps=..., checkpoint_freq=..., eval_freq=..., log_dir="runs/...", exp_name=...)`.
           - Bundles all saved checkpoints, `metrics.csv`, and run metadata into `artifacts.tar.gz` and calculates SHA256 checksums written to `checksums.sha256`.
       - D-02: `package_kernel_directory(target_dir: str, metadata: dict, entrypoint_code: str, source_root: str) -> str`:
         - Writes `kernel-metadata.json`, `remote_entrypoint.py`, and compresses local `src/tqc` into `tqc_source.tar.gz` inside `target_dir`.
       - D-13: `run_local_preflight_smoke(env_id: str = "HalfCheetah-v4", steps: int = 100, device: str = "cpu") -> bool`:
         - Runs a fast 100-step local dry run using `train_tqc` to confirm environment creation, tensor arithmetic, and checkpoint writing succeed without error before remote launch.
    2. Author unit tests in `tests/test_remote_packager.py`:
       - Test `build_kernel_metadata` outputs all required Kaggle fields.
       - Test `create_remote_entrypoint` contains installation, training invocation, and `.tar.gz` archive creation.
       - Test `package_kernel_directory` correctly populates destination directory with valid files.
       - Test `run_local_preflight_smoke` completes within seconds and generates valid test checkpoint.
  </action>
  <verify>
    <automated>python -m unittest tests/test_remote_packager.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
  <done>
    `kaggle_packager.py` generates valid kernel metadata, remote execution entrypoint, bundles source code, and validates local pre-flight smoke execution.
  </done>
</task>

</tasks>

<artifacts_produced>
### Artifacts this phase produces
- Modules: `src/tqc/remote/__init__.py`, `src/tqc/remote/kaggle_auth.py`, `src/tqc/remote/kaggle_packager.py`
- Functions: `resolve_kaggle_credentials`, `validate_kaggle_credentials`, `ensure_kaggle_cli_installed`, `build_kernel_metadata`, `create_remote_entrypoint`, `package_kernel_directory`, `run_local_preflight_smoke`
- Classes / Exceptions: `KaggleAuthError`, `KagglePackagingError`
- Test Suites: `tests/test_remote_auth.py`, `tests/test_remote_packager.py`
</artifacts_produced>

<verification>
Automated test gates:
- `python -m unittest tests/test_remote_auth.py`
- `python -m unittest tests/test_remote_packager.py`
</verification>
