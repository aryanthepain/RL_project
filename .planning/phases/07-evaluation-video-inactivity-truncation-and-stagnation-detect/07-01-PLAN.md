---
phase: 07-evaluation-video-inactivity-truncation-and-stagnation-detect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tqc/visualize.py
  - tests/test_visualize_truncation.py
autonomous: true
requirements:
  - Issue #8
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01 & D-02: RolloutInactivityDetector detects forward speed |v_x| < 0.05 m/s for 30 consecutive steps or rolling window displacement |x_t - x_{t-W}| < 0.10m with std(v_x) < 0.01"
    - "D-03: Environment step loop runs full max_steps so cumulative returns and step counts remain uncorrupted"
    - "D-04: env.render() is skipped after truncation triggers, achieving ~70x speedup without state/physics divergence"
    - "D-05: Return dictionary includes explicit metadata: truncated_at_step, total_sim_steps, is_truncated, completion_reason"
    - "D-06: Configurable warmup grace period (--warmup-steps default 60) suppresses truncation during initial spawn/drop"
    - "D-07: Configurable trailing padding frames (default 15) preserved after truncation to avoid abrupt cuts"
    - "D-15: Deterministic unit tests in tests/test_visualize_truncation.py verify stationary, stagnant, and active rollouts"
  artifacts:
    - src/tqc/visualize.py
    - tests/test_visualize_truncation.py
---

<objective>
Implement the core inactivity and stagnation detection engine, render-skipping optimization, and metadata-rich rollout recording in `src/tqc/visualize.py`:
1. D-01 & D-02: Create `RolloutInactivityDetector` supporting dual-metric detection (velocity magnitude $|v_x| < \text{speed\_threshold}$ and rolling window stagnation variance) with multi-tiered velocity resolution across MuJoCo environments (`info['x_velocity']` $\to$ `env.unwrapped.data.qvel[0]` $\to$ state coordinate displacement).
2. D-03, D-04, D-05, D-06, D-07: Integrate detection into `rollout_checkpoint_with_telemetry` with `--warmup-steps` grace period (default 60), trailing padding (default 15), and post-truncation `env.render()` skipping while running the simulation loop to full `max_steps`. Return explicit metadata (`truncated_at_step`, `total_sim_steps`, `is_truncated`, `completion_reason`).
3. D-15: Author comprehensive deterministic unit tests in `tests/test_visualize_truncation.py` testing stationary, stagnant, active, and custom fallback rollouts.

Purpose: Eliminate redundant video frames and provide massive simulation speedups without corrupting RL benchmark return metrics.
Output: Tested truncation detection engine in `src/tqc/visualize.py` and unit test suite in `tests/test_visualize_truncation.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
@.planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Inactivity & Stagnation Detector and Multi-Tiered Velocity Resolver</name>
  <files>src/tqc/visualize.py</files>
  <read_first>
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-RESEARCH.md
    src/tqc/visualize.py
  </read_first>
  <acceptance_criteria>
    - D-01: `extract_forward_velocity(info, env)` resolves `x_velocity` / `forward_velocity`, `env.unwrapped.data.qvel[0]`, or state displacement norm.
    - D-01 & D-02: `RolloutInactivityDetector` class supports `speed_threshold`, `stagnation_delta`, `patience`, `padding_frames`, and `warmup_steps`.
    - D-01: Detector identifies stationary behavior (`|v_x| < speed_threshold` for consecutive patience steps) and stagnation (`net_disp < 0.10` and `std(v) < stagnation_delta` over rolling window).
    - D-06 & D-07: Warmup grace period suppresses detection until `step >= warmup_steps`, and trailing padding frames are counted down before halting.
  </acceptance_criteria>
  <action>
    1. In `src/tqc/visualize.py`, implement helper `extract_forward_velocity(info: dict, env: Any) -> float`:
       - Check `x_velocity` in `info` (combine with `y_velocity` as Euclidean norm $\sqrt{v_x^2 + v_y^2}$ if present, e.g. for Ant-v4).
       - Fall back to `info.get('forward_velocity')` if present.
       - Fall back to `env.unwrapped.data.qvel[0]` (direct MuJoCo root joint velocity).
       - Fall back to 0.0 if unavailable.
    2. Implement `extract_forward_position(info: dict, env: Any) -> Optional[float]`:
       - Check `x_position` in `info` or `env.unwrapped.data.qpos[0]`.
    3. Implement `RolloutInactivityDetector`:
       - Initialize with `speed_threshold=0.05`, `stagnation_delta=0.01`, `patience=30`, `padding_frames=15`, `warmup_steps=60`, `window_size=30`.
       - Maintain `collections.deque(maxlen=window_size)` for velocities and positions.
       - Track `is_truncated: bool`, `completion_reason: str` ('max_steps', 'inactivity_truncated', 'stagnation_truncated', 'env_terminated'), `remaining_padding: int`, `trigger_step: Optional[int]`.
       - Method `step(step_idx: int, velocity: float, position: Optional[float] = None) -> bool`:
         - Returns `True` if frame recording should continue, `False` when frame recording has finished (including trailing padding).
         - During warmup (`step_idx < warmup_steps`), records velocity into window but does not trigger truncation.
         - Once triggered, decrements `remaining_padding` until 0, then returns `False`.
  </action>
  <verify>
    <automated>python -c "from src.tqc.visualize import RolloutInactivityDetector; d = RolloutInactivityDetector(warmup_steps=5, patience=3, padding_frames=2); [d.step(i, 0.0) for i in range(5)]; assert not d.is_truncated; [d.step(i, 0.0) for i in range(5, 8)]; assert d.is_truncated and d.completion_reason == 'inactivity_truncated'; print('Detector verified!')"</automated>
    <fails_when>assertion failure or non-zero exit code</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate Truncation & Render Skipping into rollout_checkpoint_with_telemetry with Unit Tests</name>
  <files>src/tqc/visualize.py, tests/test_visualize_truncation.py</files>
  <read_first>
    src/tqc/visualize.py
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - D-03: `rollout_checkpoint_with_telemetry` runs simulation for full `max_steps` so `total_return`, `rewards`, and `velocities` reflect complete rollout.
    - D-04: `env.render()` is called only while recording is active; once truncation and padding finish, `env.render()` is skipped for subsequent steps.
    - D-05: Return dict contains `truncated_at_step`, `total_sim_steps`, `is_truncated`, `completion_reason`, `frames` (truncated + padded length), and full-length metric arrays.
    - D-15 & D-16: `tests/test_visualize_truncation.py` passes deterministically with 100% test success across active, stationary, stagnant, and disabled truncation rollouts.
  </acceptance_criteria>
  <action>
    1. Update `rollout_checkpoint_with_telemetry` signature in `src/tqc/visualize.py`:
       - Add optional kwargs with defaults:
         - `truncate_inactive: bool = True`
         - `speed_threshold: float = 0.05`
         - `stagnation_delta: float = 0.01`
         - `patience: int = 30`
         - `padding_frames: int = 15`
         - `warmup_steps: int = 60`
    2. Inside `rollout_checkpoint_with_telemetry`:
       - Instantiate `RolloutInactivityDetector` if `truncate_inactive` is True.
       - Maintain `recording_active = True`.
       - In step loop `for step in range(max_steps)`:
         - Only call `env.render()` if `render and recording_active`.
         - Execute `env.step(action)` regardless of `recording_active`.
         - Extract `v_x` and `pos_x` using helper functions.
         - If `truncate_inactive`:
           - Update detector with `(step, v_x, pos_x)`.
           - If detector signals recording is finished (`step(...)` returns False), set `recording_active = False`.
         - If `recording_active` and raw frame was rendered:
           - Render HUD overlay (pass status pill if `detector.is_truncated`).
           - Append to `frames`.
         - Record `v_x`, `reward`, `action`, `q_val` to full metric lists.
       - Populate return dictionary with:
         - `truncated_at_step`: `detector.trigger_step` if truncated else None.
         - `total_sim_steps`: step + 1.
         - `is_truncated`: `detector.is_truncated` if `truncate_inactive` else False.
         - `completion_reason`: `detector.completion_reason` (or 'env_terminated' / 'max_steps').
         - `frames`, `velocities`, `rewards`, `cumulative_returns`, `actions`, `q_means`, `total_return`.
    3. Create `tests/test_visualize_truncation.py`:
       - Test Mock Environment with stationary agent (zero velocity): verifies frames length equals `warmup_steps + patience + padding_frames` (e.g. 10 + 5 + 3 = 18 frames) while `len(velocities) == max_steps` (50).
       - Test Mock Environment with active agent (high velocity): verifies full frames recorded (50 frames) with `is_truncated == False`.
       - Test Mock Environment with stagnant agent (oscillating velocity with zero net displacement): verifies stagnation truncation triggers with reason `'stagnation_truncated'`.
       - Test `truncate_inactive=False`: verifies full frame capture even with zero velocity.
       - Test return dictionary keys and metadata integrity.
  </action>
  <verify>
    <automated>python -m unittest tests/test_visualize_truncation.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
</task>

</tasks>

<must_haves>
- Inactivity and stagnation detection logic runs cleanly without modifying environment physics or episode returns.
- Full metric arrays (velocities, rewards, returns, q_means) are preserved in the return dictionary.
- Automated unit test suite `tests/test_visualize_truncation.py` passes deterministically with 0 failures.
</must_haves>
