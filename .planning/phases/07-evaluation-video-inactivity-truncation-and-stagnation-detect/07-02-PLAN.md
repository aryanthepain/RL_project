---
phase: 07-evaluation-video-inactivity-truncation-and-stagnation-detect
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/tqc/visualize.py
  - tests/test_visualize_grid_truncation.py
autonomous: true
requirements:
  - Issue #8
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-08 & D-09: draw_telemetry_hud renders an amber status badge [TRUNCATED] and displays final simulation return when status is provided"
    - "D-10: create_progression_grid pads truncated panels by holding their final frame with the 3D scene dimmed by 50% while keeping the top HUD bar at 100% brightness"
    - "D-11 & D-12: create_progression_reel transitions instantly to next checkpoint without title slides, relying purely on per-frame HUD"
    - "D-13 & D-14: visualize.py exposes --no-truncate, --speed-threshold, --patience, --padding-frames, --warmup-steps, and --stagnation-delta CLI flags"
    - "D-15: Integration tests verify grid composition and end-to-end rollout on HalfCheetah-v4"
  artifacts:
    - src/tqc/visualize.py
    - tests/test_visualize_grid_truncation.py
---

<objective>
Enhance the visualization presentation layer with HUD badges, selective grid dimming, progression reel synchronization, and comprehensive CLI interfaces:
1. D-08 & D-09: Update `draw_telemetry_hud` in `src/tqc/visualize.py` to support drawing high-contrast amber status badges (`[TRUNCATED: INACTIVE]`, `[TRUNCATED: STAGNANT]`) and final simulation episode returns (`FINAL EP RET: XXX.X`) during trailing padding frames and the final frame.
2. D-10 & D-11: Update `create_progression_grid` to detect truncated panels with fewer frames than the longest panel, holding their final frame and applying a 50% brightness reduction to the 3D canvas while preserving the top HUD banner at full brightness. Ensure `create_progression_reel` cuts immediately between checkpoints without artificial delays.
3. D-13 & D-14: Expose all 6 CLI arguments (`--no-truncate`, `--speed-threshold`, `--patience`, `--padding-frames`, `--warmup-steps`, `--stagnation-delta`) in `src/tqc/visualize.py` `main()` entrypoint.
4. D-15: Author integration test suite in `tests/test_visualize_grid_truncation.py` verifying grid composition, selective dimming, and real `HalfCheetah-v4` rollout execution.

Purpose: Deliver visually stunning, intuitive, and synchronized presentation videos across checkpoints while providing complete CLI ergonomics.
Output: Completed visual features in `src/tqc/visualize.py` and integration tests in `tests/test_visualize_grid_truncation.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
@.planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-RESEARCH.md
@.planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HUD Badges, 50% Selective Grid Dimming & Reel Synchronization</name>
  <files>src/tqc/visualize.py</files>
  <read_first>
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-RESEARCH.md
    src/tqc/visualize.py
  </read_first>
  <acceptance_criteria>
    - D-08 & D-09: `draw_telemetry_hud` accepts optional `status_text: Optional[str]` and `final_return: Optional[float]`.
    - D-08: When `status_text` is provided, draws an amber badge pill next to the step counter with white bold text.
    - D-09: When `final_return` is provided, displays `FINAL EP RET: {final_return:.1f}`.
    - D-10: `create_progression_grid` and `tile_grid_frames` detect panels shorter than the maximum length, extending them by repeating their final frame with environment canvas dimmed by 50% below the HUD banner (`banner_height`).
    - D-11: `create_progression_reel` concatenates checkpoint rollout frame arrays sequentially without adding extra dead-frame holds.
  </acceptance_criteria>
  <action>
    1. In `src/tqc/visualize.py`, update `draw_telemetry_hud`:
       - Add kwargs: `status_text: Optional[str] = None`, `final_return: Optional[float] = None`.
       - If `status_text` is set:
         - Draw an amber pill `(217, 119, 6)` in the banner next to the step counter with anti-aliased text.
       - If `final_return` is set:
         - Draw `FINAL EP RET: {final_return:+.1f}` in place of or adjacent to live return.
    2. Implement helper `dim_environment_scene(frame: np.ndarray, banner_height: int = 40, dim_factor: float = 0.5) -> np.ndarray`:
       - Deep copy frame.
       - Multiply `frame[banner_height:, :, :]` by `dim_factor` and cast to `np.uint8`.
       - Return composite frame with top banner untouched.
    3. Update `create_progression_grid`:
       - Find `max_frames = max(len(d["frames"]) for d in rollout_results)`.
       - For each rollout result with `len(d["frames"]) < max_frames`:
         - Take the last frame `last_frame = d["frames"][-1]`.
         - Create dimmed version using `dim_environment_scene(last_frame)`.
         - Pad list with the dimmed frame until its length matches `max_frames`.
    4. Verify `create_progression_reel`:
       - Concatenates the truncated lists directly, preserving the rapid chronological transition between checkpoints.
  </action>
  <verify>
    <automated>python -c "import numpy as np; from src.tqc.visualize import dim_environment_scene, draw_telemetry_hud; f = np.zeros((480, 640, 3), dtype=np.uint8); f[100:200, 100:200] = 200; hud = draw_telemetry_hud(f, 1, 100, 100, 10000, 1.0, 0.0, 50.0, 10.0, status_text='[TRUNCATED]', final_return=75.5); dimmed = dim_environment_scene(hud, banner_height=40, dim_factor=0.5); assert np.all(dimmed[:40] == hud[:40]); assert np.all(dimmed[100:200, 100:200] == 100); print('HUD badge and dimming verified!')"</automated>
    <fails_when>assertion failure or non-zero exit code</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Expose Truncation CLI Interface & Implement End-to-End Integration Tests</name>
  <files>src/tqc/visualize.py, tests/test_visualize_grid_truncation.py</files>
  <read_first>
    src/tqc/visualize.py
    .planning/phases/07-evaluation-video-inactivity-truncation-and-stagnation-detect/07-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - D-13 & D-14: `main()` in `src/tqc/visualize.py` exposes `--no-truncate` (disables truncation), `--speed-threshold` (default: 0.05), `--patience` (default: 30), `--padding-frames` (default: 15), `--warmup-steps` (default: 60), and `--stagnation-delta` (default: 0.01).
    - D-14: Running `python src/tqc/visualize.py --help` shows all 6 arguments with accurate help descriptions and defaults.
    - D-15: `tests/test_visualize_grid_truncation.py` verifies:
      1. Grid assembly with mixed length rollouts (short truncated panel + long panel) produces equal-dimension synchronized video with dimmed freeze-frame extension.
      2. Progression reel concatenates frames without crashing or visual popping.
      3. Real MuJoCo integration test with `HalfCheetah-v4` verifies short rollout truncation under a stationary dummy agent.
  </acceptance_criteria>
  <action>
    1. In `src/tqc/visualize.py` `main()`:
       - Add argparse arguments:
         - `--no-truncate`: `action="store_false", dest="truncate_inactive", default=True, help="Disable video inactivity truncation and record full max_steps"`
         - `--speed-threshold`: `type=float, default=0.05, help="Forward velocity magnitude threshold below which agent is considered inactive"`
         - `--patience`: `type=int, default=30, help="Consecutive inactive steps required to trigger truncation"`
         - `--padding-frames`: `type=int, default=15, help="Trailing frames to record after truncation trigger before halting"`
         - `--warmup-steps`: `type=int, default=60, help="Initial steps to ignore inactivity, allowing robot spawn and motion attempt"`
         - `--stagnation-delta`: `type=float, default=0.01, help="Velocity standard deviation threshold for in-place stagnation detection"`
       - Pass these options to `rollout_checkpoint_with_telemetry`.
    2. Create `tests/test_visualize_grid_truncation.py`:
       - Test `create_progression_grid` with dummy synthetic frames where panel 1 has 30 frames and panel 2 has 90 frames:
         - Verifies output grid video has 90 frames.
         - Verifies frames 30..89 in panel 1 are dimmed by 50% while HUD stays at full brightness.
       - Test `create_progression_reel` concatenates synthetic frames.
       - Integration test with `HalfCheetah-v4` (short 100-step test):
         - Use a stationary agent policy (action = 0).
         - Verify `rollout_checkpoint_with_telemetry` executes with `truncate_inactive=True`, triggers truncation, records truncated frame list (< 100 frames), preserves `len(rewards) == 100`, and sets `is_truncated == True`.
    3. Run full test suite:
       - `python -m unittest tests/test_visualize_truncation.py tests/test_visualize_grid_truncation.py`
  </action>
  <verify>
    <automated>python src/tqc/visualize.py --help && python -m unittest tests/test_visualize_grid_truncation.py</automated>
    <fails_when>non-zero exit code or failed tests</fails_when>
  </verify>
</task>

</tasks>

<must_haves>
- CLI flags are fully documented and functional in `src/tqc/visualize.py`.
- Tiled grid composition correctly extends truncated panels using 50% dimmed frames without blacking out or dropping frames.
- Automated integration test suite passes deterministically with 0 errors.
</must_haves>
