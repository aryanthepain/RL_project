---
phase: 08-multi-tier-compute-hierarchy-and-google-colab-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - scripts/clean_runs.py
  - notebooks/colab_tqc_benchmark.ipynb
  - docs/remote_execution.md
  - tests/test_clean_runs.py
  - tests/test_colab_notebook.py
autonomous: true
requirements:
  - Issue #4
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "D-01: notebooks/colab_tqc_benchmark.ipynb detects Colab, clones active repo branch, and installs editable package with MuJoCo sanity test"
    - "D-02 & D-03: Colab notebook auto-mounts Google Drive, persists checkpoints/metrics to Drive with folder deletion crash resilience, and falls back to local buffer with one-click zip download"
    - "D-04: scripts/clean_runs.py provides CLI and notebook-invocable disk inspection, checkpoint pruning, and run cleanup with dry-run support"
    - "D-09 & D-10: Colab notebook form widgets support environment dropdown (HalfCheetah, Hopper, Walker2d, Ant, Humanoid) and presets (smoke, quick, full)"
    - "D-12: Colab notebook configures headless EGL rendering (MUJOCO_GL=egl) to avoid GLFW display context crashes"
    - "D-13 & D-14: Colab notebook provides live matplotlib evaluation curves and embedded HTML5 video playback with Phase 7 inactivity truncation"
    - "Automated tests in tests/test_clean_runs.py and tests/test_colab_notebook.py verify pruning logic and notebook validity"
  artifacts:
    - scripts/clean_runs.py
    - notebooks/colab_tqc_benchmark.ipynb
    - docs/remote_execution.md
    - tests/test_clean_runs.py
    - tests/test_colab_notebook.py
---

<objective>
Deliver the complete Google Colab benchmark training notebook, in-notebook evaluation video player with Phase 7 inactivity truncation, live plotting, disk quota pruning utility, and updated documentation:
1. D-04: Implement `scripts/clean_runs.py` enabling users to inspect disk usage across `runs/` or Google Drive `tqc_runs/`, prune intermediate checkpoints (retaining `best_model.pt` + `latest.pt`), and delete old runs.
2. D-01, D-02, D-03, D-09, D-10, D-12, D-13, D-14: Build `notebooks/colab_tqc_benchmark.ipynb` featuring headless EGL GPU setup, hybrid Git clone, Drive mounting with folder deletion crash resilience, interactive form widgets, training cell with auto-resume, live matplotlib curves, embedded HTML5 video player with Phase 7 inactivity truncation, Drive quota pruning cell, and one-click zip export.
3. Author automated notebook validation tests in `tests/test_colab_notebook.py` and unit tests in `tests/test_clean_runs.py`, and update `docs/remote_execution.md`.

Purpose: Enable anyone to train TQC agents on free Google Colab GPUs with robust storage resilience, instant video feedback, and zero-headache Drive management.
Output: Validated notebook in `notebooks/`, pruning utility in `scripts/`, updated docs in `docs/`, and passing tests in `tests/`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
@.planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-RESEARCH.md
@.planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-01-PLAN.md
@src/tqc/visualize.py
@docs/remote_execution.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Disk Quota Inspection & Checkpoint Pruning Utility</name>
  <files>scripts/clean_runs.py, tests/test_clean_runs.py</files>
  <read_first>
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - D-04: `scripts/clean_runs.py` scans a specified root directory (`runs/` or Drive `tqc_runs/`).
    - D-04: Supports mode `--inspect`: calculates total size in MB/GB, size per run, and count of checkpoints.
    - D-04: Supports mode `--prune`: deletes intermediate `checkpoint_*.pt` files while strictly preserving `best_model.pt`, `latest.pt`, `final_model.pt`, `metrics.csv`, and video files.
    - D-04: Supports mode `--delete --run <run_id>`: removes an entire specific run directory with confirmation.
    - D-04: Supports `--dry-run`: previews files to be deleted and space saved without deleting anything.
    - `tests/test_clean_runs.py` tests all modes on temporary mock run directories.
  </acceptance_criteria>
  <action>
    1. Create `scripts/clean_runs.py`:
       - Argparse CLI with `--dir` (default: `runs`), `--inspect`, `--prune`, `--delete`, `--run <id>`, `--dry-run`, `--yes`.
       - Implement functions:
         - `scan_run_directories(root_dir: str) -> List[RunInfo]`
         - `prune_intermediate_checkpoints(run_dir: str, dry_run: bool = False) -> Tuple[int, int]` (returns count and bytes freed)
         - `format_inspection_table(runs: List[RunInfo]) -> str`
       - Ensure safe deletion logic that never removes `best_model.pt`, `latest.pt`, `final_model.pt`, or `metrics.csv`.
    2. Author `tests/test_clean_runs.py`:
       - Create mock run directories with dummy `checkpoint_100.pt`, `checkpoint_200.pt`, `best_model.pt`, `latest.pt`, `metrics.csv`.
       - Test `--inspect` output parsing.
       - Test `--prune` with `--dry-run` (files preserved).
       - Test `--prune` live (intermediate files deleted, best and latest preserved).
       - Test `--delete` on a single run.
  </action>
  <verify>
    <automated>python -m unittest tests/test_clean_runs.py</automated>
    <fails_when>any test fails</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build Self-Contained Google Colab Benchmark Notebook</name>
  <files>notebooks/colab_tqc_benchmark.ipynb</files>
  <read_first>
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-RESEARCH.md
    src/tqc/visualize.py
    scripts/run_experiments.py
  </read_first>
  <acceptance_criteria>
    - `notebooks/` directory created with `colab_tqc_benchmark.ipynb`.
    - D-01: Header contains "Open in Colab" badge and markdown setup instructions.
    - D-12: Setup cell configures `export MUJOCO_GL=egl`, installs Linux packages (`libglew-dev`, `patchelf`, `ffmpeg`), and verifies GPU.
    - D-01: Repo cell clones `aryanthepain/RL_project` (active branch) and runs editable install `pip install -e .` with 1-second MuJoCo sanity check.
    - D-02 & D-03: Google Drive mount cell with folder deletion crash resilience and local buffer fallback.
    - D-09 & D-10: Form widgets with parameters:
      - `ENVIRONMENT`: dropdown `["HalfCheetah-v4", "Hopper-v4", "Walker2d-v4", "Ant-v4", "Humanoid-v4"]`
      - `PRESET`: dropdown `["smoke", "quick", "full"]`
      - `SEED`: integer parameter (default 42)
      - `AUTO_RESUME`: boolean parameter (default True)
      - `USE_DRIVE`: boolean parameter (default True)
    - D-11: Training execution cell invokes training with auto-resumption and safe checkpoint saving.
    - D-14: Live plotting cell renders evaluation curves (`eval_return_mean`, actor/critic losses, alpha).
    - D-13: In-notebook video playback cell uses `src/tqc/visualize.py` with `--truncate-inactive` and embeds HTML5 video player (`IPython.display.Video`).
    - D-04: Drive quota cleanup cell invokes `clean_runs.py` to inspect or prune checkpoints.
    - D-02: Zip export cell bundles artifacts into `.tar.gz` and triggers browser download for unmounted sessions.
  </acceptance_criteria>
  <action>
    1. Ensure `notebooks/` directory exists.
    2. Construct `notebooks/colab_tqc_benchmark.ipynb` using a generator script or direct valid Jupyter Notebook JSON schema (v4):
       - Markdown cells formatted professionally with clear headings, badges, and code blocks.
       - Code cells containing all required logic with `#@title` and `#@param` Colab annotations.
       - Ensure all Python code in cells is syntactically valid and handles both Colab and local environments gracefully.
  </action>
  <verify>
    <automated>python -c "import json; nb = json.load(open('notebooks/colab_tqc_benchmark.ipynb', encoding='utf-8')); assert nb['nbformat'] == 4; assert len(nb['cells']) >= 8; print(f'Notebook valid with {len(nb[\"cells\"])} cells!')"</automated>
    <fails_when>invalid JSON or cell count below 8</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Author Notebook Structural Tests and Update Remote Execution Documentation</name>
  <files>tests/test_colab_notebook.py, docs/remote_execution.md</files>
  <read_first>
    notebooks/colab_tqc_benchmark.ipynb
    docs/remote_execution.md
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `tests/test_colab_notebook.py` validates:
      1. Valid Jupyter notebook JSON structure (nbformat 4).
      2. Code cell Python syntax compilation without `SyntaxError`.
      3. Presence of mandatory cells: EGL configuration (`MUJOCO_GL=egl`), Git clone, Drive mount, parameter form widgets, training cell, video rendering cell, pruning cell.
    - `docs/remote_execution.md` updated to detail Tier 2 Google Colab execution, Drive folder deletion crash resilience, checkpoint auto-resumption, and pruning tool usage.
    - All unit tests pass deterministically via `python -m unittest tests/test_clean_runs.py tests/test_colab_notebook.py`.
  </acceptance_criteria>
  <action>
    1. Author `tests/test_colab_notebook.py`:
       - Load `notebooks/colab_tqc_benchmark.ipynb`.
       - For each code cell, strip Colab-specific magic commands (`!`, `%`, `#@`) and test `compile(code, '<cell>', 'exec')`.
       - Assert presence of key invariants (`MUJOCO_GL`, `drive.mount`, `clean_runs.py`, `rollout_checkpoint_with_telemetry`).
    2. Update `docs/remote_execution.md`:
       - Add Section 5: "Tier-2: Google Colab GPU Workflow".
       - Document Google Drive synchronization, folder deletion crash resilience, in-notebook video playback with Phase 7 inactivity truncation, and storage pruning with `scripts/clean_runs.py`.
       - Add Section 6: "Multi-Tier Dispatcher CLI (`scripts/run_experiments.py`)".
  </action>
  <verify>
    <automated>python -m unittest tests/test_clean_runs.py tests/test_colab_notebook.py</automated>
    <fails_when>any test fails</fails_when>
  </verify>
</task>

</tasks>
