---
phase: 08-multi-tier-compute-hierarchy-and-google-colab-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tqc/compute/__init__.py
  - src/tqc/compute/tier_detector.py
  - src/tqc/compute/dispatcher.py
  - src/tqc/train.py
  - scripts/run_experiments.py
  - tests/test_tier_detector.py
  - tests/test_compute_dispatcher.py
autonomous: true
requirements:
  - Issue #4
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 4
  confidence: high
must_haves:
  truths:
    - "D-05: Strict compute priority hierarchy resolves in order: Tier 1: Kaggle > Tier 2: Colab > Tier 3: Local CUDA GPU > Tier 4: Local CPU fallback"
    - "D-06 & D-07: TierDetector probes environment credentials, Colab runtime, CUDA GPU specs, and CPU threads, returning structured TierCapability and formatted ASCII status table"
    - "D-08: Transparent fallback telemetry logs clear warning banners explaining why any higher tier was skipped"
    - "D-09 & D-10: Unified CLI scripts/run_experiments.py supports --tier auto|kaggle|colab|gpu|cpu, --env <id>, and presets (smoke, quick, full)"
    - "D-11: src/tqc/train.py automatically resumes training from existing checkpoint_*.pt or latest.pt without resetting step count to 0"
    - "D-03: src/tqc/train.py checkpoint saving defensively auto-recreates directories on FileNotFoundError, ensuring user deletion of Drive folder does not crash active training"
    - "Unit and integration tests in tests/test_tier_detector.py and tests/test_compute_dispatcher.py pass deterministically"
  artifacts:
    - src/tqc/compute/tier_detector.py
    - src/tqc/compute/dispatcher.py
    - scripts/run_experiments.py
    - tests/test_tier_detector.py
    - tests/test_compute_dispatcher.py
---

<objective>
Build the multi-tier compute detection engine, dispatcher, checkpoint resumption, and unified experiment runner CLI:
1. D-05, D-06, D-07, D-08: Implement `TierDetector` in `src/tqc/compute/tier_detector.py` to probe Kaggle credentials, Colab runtime, CUDA GPU, and CPU threads, rendering an ASCII summary table and emitting clear fallback warning banners.
2. D-03, D-11: Integrate checkpoint resumption (`resume_checkpoint` / auto-resume) and defensive directory auto-recreation (Drive folder deletion crash resilience) into `src/tqc/train.py`.
3. D-06, D-09, D-10: Implement `ComputeDispatcher` in `src/tqc/compute/dispatcher.py` and expose the unified CLI runner in `scripts/run_experiments.py` with presets (`smoke`, `quick`, `full`), environment targeting (`--env`), and interactive tier inspection.
4. Author comprehensive deterministic unit and integration tests in `tests/test_tier_detector.py` and `tests/test_compute_dispatcher.py`.

Purpose: Provide an intelligent, automated execution pipeline that selects the best available compute tier across all MuJoCo benchmarks.
Output: Tested multi-tier compute subsystem in `src/tqc/compute/`, CLI in `scripts/run_experiments.py`, and test suites in `tests/`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
@.planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-RESEARCH.md
@src/tqc/remote/kaggle_auth.py
@src/tqc/remote/queue_manager.py
@src/tqc/train.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Multi-Tier Detection Engine and ASCII Status Table</name>
  <files>src/tqc/compute/__init__.py, src/tqc/compute/tier_detector.py</files>
  <read_first>
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-RESEARCH.md
    src/tqc/remote/kaggle_auth.py
  </read_first>
  <acceptance_criteria>
    - D-05: `ComputeTier` enum defines `KAGGLE = "kaggle"`, `COLAB = "colab"`, `LOCAL_GPU = "gpu"`, `LOCAL_CPU = "cpu"`.
    - D-06 & D-07: `TierCapability` dataclass stores `tier`, `available: bool`, `name: str`, `details: dict`, `reason: Optional[str]`.
    - D-07: `TierDetector.probe_all()` probes:
      1. Kaggle (via `resolve_kaggle_credentials(prompt_if_missing=False)` and validating credentials).
      2. Colab (`'google.colab' in sys.modules` or runtime check).
      3. Local GPU (`torch.cuda.is_available()`, device name, VRAM in GB).
      4. Local CPU (`os.cpu_count()`, torch thread pool count).
    - D-05: `resolve_highest_tier()` selects the highest available tier respecting the strict hierarchy (Kaggle > Colab > Local GPU > Local CPU).
    - D-07 & D-08: `format_summary_table()` outputs a clean ASCII table with status badges (`[READY]`, `[SKIPPED]`, `[UNAVAILABLE]`), hardware details, and fallback explanations.
  </acceptance_criteria>
  <action>
    1. Create package `src/tqc/compute/__init__.py`.
    2. Create `src/tqc/compute/tier_detector.py`:
       - Define `ComputeTier(str, Enum)` and `TierCapability` dataclass.
       - Implement `TierDetector` class with configurable probers:
         - `probe_kaggle() -> TierCapability`
         - `probe_colab() -> TierCapability`
         - `probe_local_gpu() -> TierCapability`
         - `probe_local_cpu() -> TierCapability`
         - `probe_all() -> Dict[ComputeTier, TierCapability]`
         - `resolve_highest_tier(probed: Optional[Dict[ComputeTier, TierCapability]] = None) -> ComputeTier`
         - `format_summary_table(probed: Optional[Dict[ComputeTier, TierCapability]] = None, selected_tier: Optional[ComputeTier] = None) -> str`
       - Ensure all probes are exception-safe and never raise on missing dependencies or missing hardware.
  </action>
  <verify>
    <automated>python -c "from src.tqc.compute.tier_detector import TierDetector, ComputeTier; d = TierDetector(); res = d.probe_all(); assert ComputeTier.LOCAL_CPU in res; assert res[ComputeTier.LOCAL_CPU].available; print(d.format_summary_table()); print('Tier detector verified!')"</automated>
    <fails_when>assertion failure or non-zero exit code</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate Checkpoint Resumption & Drive Deletion Crash Resilience into train_tqc</name>
  <files>src/tqc/train.py</files>
  <read_first>
    src/tqc/train.py
    src/tqc/agent.py
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - D-11: `train_tqc` accepts `resume: bool = False` and `resume_checkpoint: Optional[str] = None`.
    - D-11: If `resume=True` and `resume_checkpoint` is None, auto-detects the latest checkpoint (`checkpoint_<step>.pt` with highest step, or `latest.pt`) in `logger.exp_dir` or `os.path.join(log_dir, exp_name)`.
    - D-11: Loads model weights and optimizer states using `agent.load(...)`, recovers `start_step`, and continues the training loop from `step = start_step + 1` up to `total_timesteps`.
    - D-11: Updates `latest.pt` on each periodic checkpoint alongside `checkpoint_<step>.pt`.
    - D-03: Checkpoint saving logic catches `(FileNotFoundError, OSError)` and executes `os.makedirs(..., exist_ok=True)` defensively before saving, ensuring that external deletion of the run directory (e.g. Google Drive folder) does not crash training.
  </acceptance_criteria>
  <action>
    1. In `src/tqc/train.py`, update `train_tqc`:
       - Add parameters: `resume: bool = False`, `resume_checkpoint: Optional[str] = None`.
       - Add helper `find_latest_checkpoint(exp_dir: str) -> Optional[Tuple[str, int]]`:
         - Scans `exp_dir` for `checkpoint_*.pt` files and extracts the highest step number.
         - Falls back to `latest.pt` if present.
       - If `resume` is True or `resume_checkpoint` is provided:
         - Resolve target checkpoint path.
         - Call `agent.load(ckpt_path)`.
         - Extract `loaded_step` and set `start_step = loaded_step + 1`.
         - Log resumption banner: `f"Resuming training from step {loaded_step} ({ckpt_path})"`.
       - Defensive directory persistence:
         - Create helper `safe_save(agent: TQCAgent, path: str)` that runs `os.makedirs(os.path.dirname(path), exist_ok=True)` and wraps `agent.save(path)` in a try-except block that recovers from directory deletion.
         - In periodic checkpoints, call `safe_save(agent, ckpt_path)` and also `safe_save(agent, os.path.join(logger.exp_dir, "latest.pt"))`.
  </action>
  <verify>
    <automated>python -c "import tempfile, os, torch; from src.tqc.agent import TQCAgent; from src.tqc.train import safe_save; agent = TQCAgent(state_dim=10, action_dim=2); tmp = tempfile.mkdtemp(); ckpt = os.path.join(tmp, 'sub', 'test.pt'); safe_save(agent, ckpt); assert os.path.exists(ckpt); os.remove(ckpt); os.rmdir(os.path.dirname(ckpt)); safe_save(agent, ckpt); assert os.path.exists(ckpt); print('Safe save resilience verified!')"</automated>
    <fails_when>assertion failure or non-zero exit code</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement Compute Dispatcher and Unified Experiment Runner CLI</name>
  <files>src/tqc/compute/dispatcher.py, scripts/run_experiments.py</files>
  <read_first>
    src/tqc/compute/tier_detector.py
    src/tqc/train.py
    .planning/phases/08-multi-tier-compute-hierarchy-and-google-colab-integration/08-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - D-06: `ComputeDispatcher` dispatches workloads based on resolved tier.
    - D-08: If a requested or higher tier cannot be used, prints an informative warning banner with the reason.
    - D-09: Supports environment parameter `--env` (`HalfCheetah-v4`, `Hopper-v4`, `Walker2d-v4`, `Ant-v4`, `Humanoid-v4`).
    - D-10: Supports presets:
      - `smoke`: 100 steps (quick validation)
      - `quick`: 10,000 steps (fast functional run)
      - `full`: 1,000,000 steps (3,000,000 steps for Humanoid-v4)
    - D-06 & D-07: `scripts/run_experiments.py` provides CLI with `--tier`, `--env`, `--preset`, `--seed`, `--resume`, `--yes`, `--dry-run`.
    - In interactive TTY (when `--yes` is not set), displays ASCII summary table and asks user for confirmation before dispatching.
  </acceptance_criteria>
  <action>
    1. Create `src/tqc/compute/dispatcher.py`:
       - Define preset mappings for environments and step counts.
       - Implement `ComputeDispatcher`:
         - `dispatch(tier: Union[ComputeTier, str], env_id: str, preset: str, seed: int = 42, resume: bool = True, dry_run: bool = False, interactive: bool = True) -> Any`
         - Tier 1 (Kaggle): delegates to `src/tqc/remote/kaggle_packager.py` and `scripts/run_remote_experiment.py`.
         - Tier 2 (Colab): if running inside Colab, executes `train_tqc`; if outside Colab, outputs instructions and Colab URL/path to run `notebooks/colab_tqc_benchmark.ipynb`.
         - Tier 3 (Local GPU): executes `train_tqc(..., device='cuda', buffer_capacity=1_000_000)` with `torch.backends.cudnn.benchmark = True`.
         - Tier 4 (Local CPU): executes `train_tqc(..., device='cpu', buffer_capacity=100_000)`.
    2. Create `scripts/run_experiments.py`:
       - Argparse CLI exposing `--tier`, `--env`, `--preset`, `--seed`, `--resume`, `--yes`, `--dry-run`, `--list-tiers`.
       - Handles terminal interaction, displays ASCII table, and dispatches.
  </action>
  <verify>
    <automated>python scripts/run_experiments.py --list-tiers && python scripts/run_experiments.py --preset smoke --env HalfCheetah-v4 --tier cpu --dry-run</automated>
    <fails_when>non-zero exit code</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Author Unit and Integration Tests for Tier Detector and Dispatcher</name>
  <files>tests/test_tier_detector.py, tests/test_compute_dispatcher.py</files>
  <read_first>
    src/tqc/compute/tier_detector.py
    src/tqc/compute/dispatcher.py
    scripts/run_experiments.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_tier_detector.py` tests:
      1. Probing with mocked Kaggle credentials (available vs unavailable).
      2. Probing with mocked CUDA (available vs unavailable).
      3. Strict priority resolution (Kaggle > Colab > Local GPU > Local CPU).
      4. Summary table rendering and badge generation.
    - `tests/test_compute_dispatcher.py` tests:
      1. Preset step resolution (`smoke`, `quick`, `full` across environments).
      2. Fallback execution to Local CPU when higher tiers are unavailable.
      3. Dry-run dispatch validation.
      4. Checkpoint auto-resumption integration test.
    - All tests pass deterministically via `python -m unittest tests/test_tier_detector.py tests/test_compute_dispatcher.py`.
  </acceptance_criteria>
  <action>
    1. Author `tests/test_tier_detector.py` using `unittest` and `unittest.mock`:
       - Mock `torch.cuda.is_available()`, `resolve_kaggle_credentials()`, `'google.colab'` module.
       - Verify each tier probe and the priority resolver.
    2. Author `tests/test_compute_dispatcher.py`:
       - Test `ComputeDispatcher` parameter adaptation across all 5 MuJoCo environments.
       - Test fallback warnings when Kaggle or CUDA is unavailable.
       - Test end-to-end `smoke` run on CPU with checkpoint saving and auto-resumption verification.
  </action>
  <verify>
    <automated>python -m unittest tests/test_tier_detector.py tests/test_compute_dispatcher.py</automated>
    <fails_when>any test fails</fails_when>
  </verify>
</task>

</tasks>
